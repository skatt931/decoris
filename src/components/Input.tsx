import styled from "styled-components";

const StyledInput = styled.input``;

const BasicInput = (): JSX.Element => {
  return <StyledInput placeholder="i'm so basic" />;
};

export default BasicInput;
